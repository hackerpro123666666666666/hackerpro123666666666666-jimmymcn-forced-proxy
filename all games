<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Retro Bowl Clone (Demo)</title>
  <style>
    body { background: #222; color: #fff; font-family: sans-serif; text-align: center; }
    h1 { margin-top: 2em; }
  </style>
</head>
<body>
  <h1>Retro Bowl Clone</h1>
  <p>This is a placeholder for an open-source Retro Bowl clone.<br>
  Replace this file with your own game if you like!</p>
</body>
</html>
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Snake Game</title>
  <style>
    body { background: #222; color: #fff; text-align: center; font-family: sans-serif; }
    canvas { background: #111; display: block; margin: 30px auto; border: 2px solid #555; }
    #score { font-size: 1.2em; margin-top: 10px; }
  </style>
</head>
<body>
  <h1>Snake</h1>
  <canvas id="game" width="400" height="400"></canvas>
  <div id="score">Score: 0</div>
  <script>
    const canvas = document.getElementById("game");
    const ctx = canvas.getContext("2d");
    const box = 20;
    let snake = [{x: 200, y: 200}];
    let direction = "RIGHT";
    let food = {
      x: Math.floor(Math.random()*20)*box,
      y: Math.floor(Math.random()*20)*box
    };
    let score = 0;
    document.addEventListener("keydown", dir);
    function dir(e) {
      if(e.key === "ArrowLeft" && direction !== "RIGHT") direction = "LEFT";
      else if(e.key === "ArrowUp" && direction !== "DOWN") direction = "UP";
      else if(e.key === "ArrowRight" && direction !== "LEFT") direction = "RIGHT";
      else if(e.key === "ArrowDown" && direction !== "UP") direction = "DOWN";
    }
    function draw() {
      ctx.fillStyle = "#111";
      ctx.fillRect(0,0,400,400);
      for(let i=0; i<snake.length; i++) {
        ctx.fillStyle = i===0 ? "#4f8cff" : "#fff";
        ctx.fillRect(snake[i].x, snake[i].y, box, box);
      }
      ctx.fillStyle = "#ff3";
      ctx.fillRect(food.x, food.y, box, box);
      // move snake
      let head = {x: snake[0].x, y: snake[0].y};
      if(direction === "LEFT") head.x -= box;
      if(direction === "RIGHT") head.x += box;
      if(direction === "UP") head.y -= box;
      if(direction === "DOWN") head.y += box;
      // game over
      if(head.x < 0 || head.x >= 400 || head.y < 0 || head.y >= 400 ||
        snake.some((s,i)=>i&&s.x===head.x&&s.y===head.y)) {
        clearInterval(game);
        ctx.fillStyle = "#f44";
        ctx.font = "30px sans-serif";
        ctx.fillText("Game Over", 110, 200);
        return;
      }
      // eat food
      if(head.x === food.x && head.y === food.y) {
        score++;
        document.getElementById("score").textContent = "Score: " + score;
        food = {
          x: Math.floor(Math.random()*20)*box,
          y: Math.floor(Math.random()*20)*box
        };
      } else {
        snake.pop();
      }
      snake.unshift(head);
      // draw score
    }
    let game = setInterval(draw, 100);
  </script>
</body>
</html>
